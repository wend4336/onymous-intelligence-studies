# 专题 · Gap 权益证明语义

**Stake Attestation Semantic of GAP（SAS）**

**版本**：v0.1（草案）｜ **依赖**：第 0 篇《命名论文》、模块一《消融穿越类型学》、模块二《具名柜台设计学》、模块四《签付动力学》、模块五《货币是对裁决结果收敛的贴现》 ｜ **性质**：具名智能研究的底层原语定义件——为整套签注生效体系提供底层理论与范式支撑。属生效层不可拆分的核心原生能力，**不对外下放、不允许第三方自定义**。

---

## 摘要 / Abstract

本文定义具名智能的底层原语：**Gap 权益证明语义（SAS）**。一次具名签注押上 stake 之后，系统必须为这次生效行为铸造一枚权益证明——它同时回答三个问题：这笔签注何时生效（具名生效）、签注者换得了哪些权益（权益锚定）、预期与世界回答之间的信任缺口如何闭合（信任缺口闭环）。本文裁定：SAS 不是四大签注子语义（事实断言／效力生效／权责归属／规则兜底）的并列第五件，而是它们的**底层生成器**——四子语义是 SAS 的投影，SAS 是四子语义的动力学底座。本文给出三功能的形式化判据、对象的两阶段铸造模型、消亡处置的定金罚则二分、以及"不下放、不自定义"的四层强制机制。

**关键词**：具名智能（Onymous Intelligence）；权益证明（Stake Attestation）；具名生效；权益锚定；信任缺口闭环；Semantic IR

---

## 1. 定义与判据

**定义 1.1（Gap 权益证明语义，SAS）**。一枚具名签注被柜台受理时，由内核铸造的、承载该次押注全部权责断言与权益清单的生效层对象。它证明三件事：签注已生效、权益已锚定于具名者、信任缺口已有闭环路径。

**判据 1.2**。一个运行中的对象实例是 SAS 证书，当且仅当：① 它由 claim-mint 原子事务铸造（非铸造不入场）；② 它的 bearer 字段非空且恒定（具名、不可转让）；③ 它引用的托管凭证（escrow_ref）真实锁定（有准备金）；④ 它的权益清单出自内核封闭枚举（语义未自定义）。四条缺一，即为伪证书。

**术语四件**（规则 7.3）：

| 口语名 | 正式名 | English | 判据 |
|---|---|---|---|
| 权益证明 | Gap 权益证明语义 | Stake Attestation Semantic of GAP (SAS) | 满足判据 1.2 四条 |
| 托管凭证 | stake 托管凭证引用 | escrow_ref | 线性、独占、至多进入一枚已入账证书 |
| 权益凭证 | 权益证明证书 | entitlement proof | 承载五项权益清单及其状态机的 SAS 对象面 |
| 闭环 | 信任缺口闭环 | trust-gap closure | 世界回答 ∧ 收敛入账 ∧ 权益兑现，三联合取 |

## 2. 定位：零号子语义

**裁定 2.1**。SAS 是四大签注子语义的**底层生成器**（零号子语义），而非并列的第五件，亦非横切注解层：

```
四子语义 = π₁…π₄(SAS) 的投影
  π_事实断言(SAS) = 判什么、依据什么（stake 三元组前两元）
  π_效力生效(SAS) = stake 冻结前置、生效触发与权益兑现的联动
  π_权责归属(SAS) = bearer、押注标的、消亡处置（追责的抵押物）
  π_规则兜底(SAS) = 超时清算分支、退单计价
```

两条否决理由：① 若并列为第五块，四块中既有的 stake 残片要么双写（双写不一致即坏账新品种）、要么搬迁（破坏四块正交性）；② SAS 天然跨值／绑定／Handle 三层（§3），而四子语义块是"值层声明＋绑定层冻结"的同类物，不同层者不可并列。SAS 之于四子语义，如 Gap 周期之于 ledger_state 状态机（模块四 §3.1）——**SAS 是动力学底座，四子语义是记账投影**。

## 3. 对象模型：三分法归层与两阶段铸造

按《编程语言三分法重读：值／绑定／Handle（引用）》，SAS 证书是三层复合对象：

| 构件 | 层 | 说明 |
|---|---|---|
| stake 三元组内容（判什么／依据什么／押上什么） | 值 | 铸造即冻结，入内容哈希 |
| bearer 绑定与权益归属规则 | 绑定 | mint 时冻结；supersedes 修订链上 bearer 恒定 |
| escrow_ref | Handle | 线性 capability：禁复制、禁无声丢弃 |
| attestation_id（`oi:att/…`） | Handle（强） | 铸造发行，永不复用，入账即永生可达 |

**两阶段铸造**：

- **T0＝claim-mint 铸壳**：绑定建立、弱引用转强、草稿落价、铸壳证书四段为一原子事务，任一失败全部回滚。stake 入托管（借记侧）与证书发行（贷记侧）双侧同时完成——参照支付清算的结算最终性结构，双侧完成即具名生效。
- **T1＝收敛 vesting**：世界回答（签付确认）触发证书状态迁移，年轮深度 d*、ξ、引用权、免审抵押资格逐项起算。T1 不写新证书，是同一证书绑定层状态的账房维护。

**两面证明规则**：escrow_ref（托管凭证，证明押注真实冻结）与权益证书（权益凭证，证明押注换得何物）1:1 互指，缺一拒铸——有托管无权益是蒸发的金融版，有权益无准备金是坏账的发行版。

**消亡处置——定金罚则二分**（参民法典第 586–588 条双向罚则结构）：

| 终态 | stake 处置 | 性质 |
|---|---|---|
| 退单（具名者主动） | 退回，计退单费并入信任折旧 δ | 退单不免费 |
| 拒签（收敛时否决） | 全额退回拒签者；被拒呈验方计虚警率 | 否决是资产，非违约 |
| 超时（世界永不回答） | 按规则兜底分支：退回／核销留痕／续冻待裁 | 核销须留痕 |
| 伪造（坏账） | 罚没 | 定金罚则的没收半 |

## 4. 三功能的形式化

**4.1 具名生效（onymous effectuation）**。证书是"生效"的可出示对象——"未入账不生效"（P3）从状态位升格为证书存在性：

```
effected(e) ≡ ∃ a ∈ SAS. minted(a) ∧ a.event_ref = e.id
            ∧ a.bearer ≠ ∅ ∧ escrow_locked(a.escrow_ref)
            ∧ e.ledger_state ∈ {挂账, 已入账}
```

**4.2 权益锚定（entitlement anchoring）**。五项权益，逐项有铸造条件、归属规则、消亡条件：

| 权益项 | 铸造 | 归属与红线 |
|---|---|---|
| ① PV（正式收敛预期价） | T0 草稿落价 | bearer 独占；可重估、**不可转让、永不兑付法币** |
| ② 年轮深度 d* | T1 收敛后起算 | 计入 bearer 复利流；坏账则隔离（interest=false） |
| ③ ξ 计入 | T1 后可报价 | B→H 跨层效率；只标在收敛结果上，永不以法币报价 |
| ④ 引用权 | T1 入年轮区激活 | **对象级权利**——被引的是对象，不是名字 |
| ⑤ 免审额度抵押资格 | 年轮累积达阈值 | 以过往年轮为抵押的信用额度；坏账吊销 |

**4.3 信任缺口闭环（trust-gap closure）**：

```
closed_loop(a) ≡ 权益兑现 ∧ 世界回答 ∧ 收敛入账   （三联合取，缺一不闭环）
```

闭环失败分两形态：**断裂**——证书显式处置为退回，账平而环未闭，Gap 重开；**伪造**——`attested(a) ∧ ¬world_answered(a)`，账上记闭合而敞口无人真实承担。后者使"伪 Gap／坏账"第一次获得可计算判据。空转定理（命题 1.2）的证书版表述随之成立：**无证书铸造的系统不积累任何权益**——Token 流的忙不产生 attestation。

## 5. 治理红线：不对外下放、不允许第三方自定义

若第三方可自定义权益语义，三条红线同时失守：可自定义"可转让权益"即破非转让核心（命题 3.2）；可自定义"法币兑付权益"即击穿单向阀门；权益类型不可枚举则坏账检出失去计算基础。**封闭枚举是可机检的前提**——这不是政策偏好，是机检可行性的边界。四层强制：

- **L1 封闭 schema**：权益类型为内核定义的封闭枚举；SAS 块是全 IR 中唯一无扩展钩子的块。开放给第三方的是"签注什么"，不开放的是"签注换来什么"。
- **L2 内核独占版本治理**：新权益类型＝内核发布新 schema 版本；已发布版本批准即冻结，存量证书永久绑定铸造时版本；版本变更本身须作为具名签署事件入账（语义治理权递归地受自家柜台管辖）。
- **L3 校验器拒铸**：白名单外字段、枚举外类型、未发布 schema 哈希、块外私设权益字段——任一命中即拒铸。
- **L4 外部锚定**：权益清单哈希入签署载荷（所签即所示）；铸造时刻配第三方可信时间戳与司法存证；合同条款锁定"语义以铸造时版本为准、变更不溯及存量、歧义不利于平台解释"。

## 6. 与货币层的一致性

命题 3.2 的四性质全部字段级编码：**可证明**（内容寻址＋第三方时间锚）、**可贴现**（贴现作用于收敛结果，不作用于名字）、**不可转让**（bearer 为修订链不变量；无 transfer 字段；escrow_ref 线性禁复制）、**不可兑付**（记账单位封闭枚举排除法币代码；schema 中不存在任何"权益→法币"出口字段——单向阀门不是政策声明，是 schema 缺席）。托管零息与年轮生息的张力由此落定：**stake 本金零息**（权益清单中无"本金生息"项），**利息只长在 d\* 上**（切断存钱生息通道、保留签署生息通道）。

## 7. 先例锚点

SAS 的每个元件都有制度史先例：定金罚则（民法典 586–588，双向押注担保生效）；不动产登记（民法典 209/214 条与 Torrens 三原则——生效原子时刻、簿优于证、登记机构首负责任）；保证金退还附孳息（招投标条例 57/58 条——权益归押注者的行政法规先例）；诉讼具结（民诉解释 110 条"拒签保证书→主张不予认定"——不落 stake 不生效力的现行法形态）；国家勋章制度（终身享有、不得出售、撤销须公告——可证明／可贴现／不可转让／不可兑付四性质的完整制度标本）；Spence 信号理论（1973——有效信号必须有成本，且成本与质量负相关，即低 stake 起步的经济学依据）。

**命名区隔声明**："Stake Attestation" 与区块链 PoS 语境的 stake（资本质押）／attestation（共识投票）仅词根相同，语义对位相反——彼为匿名生产侧的共识代谢，此为具名生效侧的裁决锚定。普通法中 affidavit 的 attestation clause（jurat）是本用法的历史正身。本体系禁用 validator／slashing 等词。

## 8. 开放问题

1. **批量会签的拆分／合并规则**：n 张卡片一次签署，铸 1 枚证书引用多 subject 还是 n 枚证书共享可拆分 escrow_ref？多人会签的权益分摊如何防责任稀释？
2. **象征性 stake 的权益档位**：微签证书的权益是否按 stake 强度分级折减？
3. **证书可否被 cite 引用为证据**：引用证书不迁移任何权益（引用的是对象不是权益）——此不变式需校验器强制。
4. **罚没去向与伪造宽限期**：罚没 stake 的归宿（坏账准备金／烧毁／呈验治理预算）；伪造判定的证据标准与公告程序（参勋章撤销公告）。
5. **柜台侧违约的反向救济**：定金罚则是双向的——柜台错误入账或错误判伪时，签注者的双倍救济语义是否写入？
6. **跨账房证书互认**：ξ 通兑要求跨内核的 schema 治理协议。
7. **T0／T1 建模**：同一证书的绑定层状态迁移，还是两枚证书加 fulfills 边——两案均不违反三分法，须拍板。

---

**回环校验**：本件补六层闭环中的**制**（柜台七件之签署入账的对象化底座），并焊接**名**（具名生效的证书化）、**动**（Gap 周期的收口凭证）、**币**（非转让核心的 schema 编码）。无悬空概念：权益证明的每一字段都可回溯到六层之一的既有命题。
