# 补证条目：外部独立收敛——Spec 与 Tickets 不持久化（Matt Pocock）

**日期**：2026-09-24
**性质**：外部补证。承《新软件：LLM 时代 Coding 与 Software 的新范式》（第七轮序章）之后，记录一次外部独立收敛事件及其对三件承重件的印证。
**证据**：《source/Spec & Tickets 不持久化的设计说明.md》（保真归档，出处与来源链见文首注记）
**印证对象**：①「法」（生效规则体系）② 消融分类学 ③ Handle 研究（值/绑定/Handle 三分法 · Gap 原语 Primitive·Host·Handle）

---

## 一、证据规格与方法论意义（A5 执行记录）

- 出处：Matt Pocock（Total TypeScript / AI Hero），X 平台认证账号 @mattpocockuk。文本为中文整理版（自述含 AI 生成内容），固化策略为**保真归档＋注记隔离**，不得冒充原帖逐字原文。
- 独立性核验：原文通篇未出现 Gap、Handle、具名、消融带、柜台等 OIS 词汇；其术语（spec、tickets、CONTEXT.md、ADR、source of truth）来自另一套工作流（mattpocock/skills）。**对方未接触我们的语言，收敛是独立发生的**——外部独立收敛是范式成立的最强证据类型，强于任何内部自洽。
- 方向性声明：其实践在前（skills 仓库与工作流已公开运行），我们的理论概括在后。本条目是双向发现的对账，不是权威引用。

## 二、对「法」的印证

1. 原文整条论证即"可匿名生产、须具名生效"的工程实例：Spec/Tickets 由 Agent 匿名自产自销（生产性动作，可委托、可重做），但**不生效**——成为长期依据只发生在代码合并这个"入账"时刻。
2. "任务完成即失效""闭合后不再迭代"是失效场景的显式化，对应签注语义中的规则兜底语义（撤销条件、失效场景写进规则，不靠默契）。
3. 第二真相源论证与"语义权失控"论证同构：两套事实互相冲突＝凭证可被单方面否认＝信任体系失效。其解法（唯一真相源必须可执行、可测试）即 A5"可呈验，不得虚构"的工程版。
4. 原则由 Matt 具名提出，非 Agent 涌现惯例——法的签发权在人，即"认领必须具名"的一次执行。

## 三、对消融分类学的印证

1. 原文的不持久化理由逐字命中具名三判据：Spec/Tickets 不可归属（推演出自谁手不重要）、可撤销（任务可重来）、无 stake（没押上任何东西）→ 无长期资产资格，应当蒸发。范式总结中"传 Ticket 的人贬值"是因为人不满足三判据；本证据证明**传 Ticket 的"物"同理贬值**——分类学从角色推广到产物。
2. 消融的时点学：消融不是禁止存在，而是禁止沉积。允许会话内与跨会话接力期间存活，闭合即消融——对应"生成、装配、状态都是可匿名可委托的代谢，价值向不可委托的具名裁决收敛"。
3. 生命周期与三池暗合：Spec/Tickets 核销（无签署、无世界回答，无年轮可沉积）；已关闭 Issue 归档（沉积为年轮，可回溯但不再作为当前开发依据＝只引用不裁决）。

## 四、对 Handle 研究的印证

1. 原文对 Tickets 的自我定位——"跨会话接力执行的临时断点标记"——就是 Handle 的定义：跨上下文可传递的访问凭证。新会话 Agent 不需要 Spec 的 binding，只需要能访问它的句柄；全部闭合即句柄丢弃、值蒸发（`f(1+2)` 里的 3）。
2. 检索污染＝死绑定病理：文件名还在（binding 在符号表），指向的值已腐烂——三分法边界案例 2（"变量声明但从未读取"）的加强版：不但不可引，还会被误引，污染后续计算。
3. 白名单＝获得持久 Handle 的四类，各落三层本体：代码（Handle+Value，可执行即绑定 SHA，可呈验）、CONTEXT.md（纯 Binding 层资产，命名契约）、ADR（签署痕迹——"取舍理由"即 stake 的记录）、已关闭 Issue（临时 ID 已 mint 为永久句柄，可 cite/trace）。
4. A2 顺势印证：Spec/Tickets 在会话与 Agent 间流动，仓库与原则不随外壳流动——Handle 流动，宿主不流动。
5. 最精确一处是 A4：落差池灰卡规格（临时 ID、只读、可登记可检索、无 Handle、无资产性表达）用于描述本文的 Spec/Tickets，几乎不用改字。

## 五、两处开放张力（裁定状态：未决，带入下一轮）

1. **CONTEXT.md 的持久化资格**：白名单中唯一没有可执行校验背书的资产（代码有测试，ADR 有具名取舍理由，Issue 有历史讨论；术语表只有"长期稳定"的自称）。按 Handle 语言它是纯 Binding 层资产：名字在，无强 Handle 指向被校验的值，可能退化为"不会腐烂的 Spec"；且防第二真相源的判据对它失效（术语表与代码冲突时谁赢，原文未说）。候选裁定：术语表条目比照 ADR 带具名签署或锚定 commit。
2. **归档判据应为"默认可不可引"，而非"存不存在"**：归档物留在默认可检索工作区＝污染源的缓刑。候选裁定（可直接回写原文处理策略的下一版）：临时 ID 不得默认转正为可解析句柄；检索默认不命中归档；只有被显式 cite 过的产物才配 mint 永久句柄。

## 六、承袭与下一步

- 承袭：《新软件》（第七轮序章）§2.4（消融带）、§2.5（具名三判据）、§2.8（对象化卡片）；《OIS 可实例化界面设计 v0.2》公理 A2/A4/A5；三分法文档边界案例 2。
- 下一步建议：① 对 §五两处张力做出裁定，结论回写为 Matt 文档处理策略的增量建议；② 本条目位置待并入《具名智能前端研究合订本》第二辑或作为第七轮补证附录，由具名者签付后生效。
