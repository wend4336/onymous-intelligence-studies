# 新软件：LLM 时代 Coding 与 Software 的新范式

## ——OIS 设计方案的范式总结（具名智能前端研究 · 第七轮序章）

**日期**：2026-09-22
**性质**：范式定位总结。以 Thorsten Ball 关于「软件开发未来」的八条判断为对照镜，论证 OIS（具名智能）前端设计——柜台、Gap 原语、呈验面——已不是旧时代的 Coding 与 Software，而是 LLM 时代的新范式。
**依据文档**：《具名智能前端研究合订本（第一辑 · 六轮）》、《OIS 可实例化界面设计 v0.2》、Thorsten Ball「软件开发未来」八条判断（用户上传《新软件》）

---

## 一、引子：一份旧范式的临终清单

Thorsten Ball 把 AI Coding 再往前推几年，列出八条判断：Code Review 会消失、单测可能消失；「写代码」弱化而「做软件」升值；Bug 变成「你让 AI 做错了东西」；Terminal、编辑器、CLI 被 Agent 吞掉；PM／Design／Engineering 分工打散；「好代码」的定义改变；Token 成为生产力门槛；UI 按需生成。

这份清单的价值不在于每条是否成真，而在于它指向同一个事实：**旧时代软件工程的每一个器官，都在失去存在的理由。** Code Review、单测、Terminal 会不会真的「死」可以争——但如果 AI Coding 继续这样发展，所有今天理所当然的习惯都值得重新问一遍：「为什么还需要它？」

OIS 六轮研究给出的回答比这份清单更进一步：**这些器官不是消失了，而是暴露了它们从未解决的真正问题。** Code Review 从来不只是「看代码对不对」，它是「谁为这次生效负责」的旧时代简陋装置；单测从来不只是「防回归」，它是「预期—回答结构」的固化形态。LLM 没有消灭这些问题，只是把它们从代码层逼到了裁决层。旧范式临终之时，正是新范式的入口。

---

## 二、逐条对照：八条判断的 OIS 重读

### 2.1 「Code Review 会消失」→ 不，它升格为柜台签付

Code Review 的旧形态——逐行看人写的代码——确实会消失，因为逐行看机器写的代码没有认知可行性。但 Code Review 的制度内核——**生效之前必经一次具名裁决**——不仅不消失，反而第一次获得了专门的设施：具名柜台。旧时代的 LGTM 橡皮图章是「有窗口无账」的病理；新范式中，每一次验收是一次签付：填单、验单、签署、入账、对账，五环齐备。Code Review 没有死，它从一种**习惯**升格为一种**制度**。

### 2.2 「写代码弱化，做软件升值」→ 动力序：定价权先于签署权

「真正值钱的是知道该解决什么问题、什么时候发布、怎么获得反馈」——用 OIS 语言精确化：值钱的是 **E[V] 的判断（这件事判对了值多少）、t 的判断（世界何时回答）、以及呈验的能力（把落差组织成可裁决的对象）**。这正是呈验面（前端的前端）的职责：定价先于签署，动力序先于建构序。旧范式把工程师的价值锚在「产出代码」上；新范式把人的价值锚在「定价与裁决」上。Ball 直觉到了方向，OIS 给出了它的金融结构：PV = E[V]/(1+r)^t。

### 2.3 「Bug 变成你让 AI 做错了东西」→ 落差的发现与认领

旧时代的 Bug 是代码与意图的落差，由代码的作者负责；新时代的「Bug」是预期与世界回答的落差——**落差（mismatch）**。OIS 的关键一刀：落差可匿名检测（LLM、Harness、世界本身皆可），但**认领必须具名**。匿名的 Gap 不是 Gap，是事故。「你让 AI 做错了」之所以是新痛点，正因为旧范式没有「认领」这个概念——没有人把这次落差登记为自己的敞口，它就在落差池里蒸发。新范式给每一条落差发存托编号（临时 ID），登记必携证据来源链（公理 A5：可呈验，不得虚构），等一个具名者来收留。

### 2.4 「Terminal、编辑器、CLI 被 Agent 吞掉」→ 消融带与可解耦前端

工具被吞掉之后，人面对的是什么？OIS 的回答：人面对的是**消融带（Ablation Zone）的界面化形态**——LLM、Harness、人三本体交界处的消融面。而消融面的设计第一次被证明是**可解耦**的：柜台与具名 Stake、Handle 是耦合的内核（信任根），前端是可解耦的外壳。Codex Harness 式浏览器、Pi Desktop 式桌面，只是同一对象模型的两个实例。旧范式里「换工具」是换工作方式；新范式里「换外壳」不动账本——宿主可换，账随 `oi:` 解析永续。

### 2.5 「分工打散，传 Ticket 的人贬值」→ 具名三判据即新分工标准

为什么「只负责传 Ticket、协调流程的人，价值快速下降」？OIS 给出了可检验的判据：因为他们**不满足具名三判据**——不可归属（Ticket 经过谁手不重要）、可撤销（流程可以重来）、无 stake（传话不押上任何东西）。新范式的分工不再按「职能」划线，而按「谁押注」划线：**会签付的人**（the Staked Attestor）成为系统的枢纽本体，其余一切——生成、装配、状态——都是可匿名、可委托的代谢。分工打散的真正含义：价值向「不可委托的具名裁决」收敛。

### 2.6 「好代码的定义会变」→ 量规可编码，品味不可转让

「很多规范本来就是为了方便人类阅读和维护」——Ball 说得对，但只说了一半。OIS 第五轮（TASTE Harness 补证）给出完整的两半：**品味的产物可以被编码为可执行契约**（量规、正例反例、禁令清单——配置流的工程化），**品味本身不可外化、不可贴现、不可转让**（命题 3.2 的品味版）。「这两个模块该不该合并」「边界到底画在哪」没有静态规则可答——这些判断保留给具名者终裁。Taste defines the direction, Harness guards the direction。新范式中「好代码」的定义确实会变，但「好判断」的定义第一次变得可记账：ξ（柜台汇率 = 单笔签署时间 / 其后 d*）高的人，即品味被账证明的人。

### 2.7 「Token 成为生产力门槛」→ 汗腺仪表批判：Token 是汗，不是年轮

这是八条中 OIS 唯一**正面反驳**的一条。Token 吞吐是 Harness 的代谢成本——它测量的是「汗」，不是「年轮」。空转定理（命题 1.2）早已宣判：无签付的系统可以很忙，但它的忙不留下任何一圈年轮。新范式的生产力门槛不是「谁能调用更多 Token」，而是**谁的系统里具名签署流不是空的**：签署密度 ρ、意外率 σ、签付复利 c(s)、柜台汇率 ξ。Pi Desktop footer 上那行 `In 80 · Out 16.0k · $0.0000` 是旧范式的汗腺仪表教科书案例；新范式的仪表回答的是「哪一次穿越留下了年轮」。

### 2.8 「UI 按需生成」→ 前端的前端：从固定界面到对象化卡片

「很多固定 UI 本质上都是人在适应不会理解自然语言的软件」——观察正确，但「UI 按需生成」的下半句 Ball 没有说：按需生成的 UI 承载什么？OIS 的回答：**承载对象，不承载功能。** 新范式的界面最小单位不是页面、不是组件，而是 Gap 卡片——Primitive（值）· Host（绑定）· Handle（引用）三位一体的可实例化对象。呈验面负责把落差生成卡片（登记＋定价＋配额），柜台负责让卡片获得宿主（签署＋mint），三池视图负责卡片的生命周期呈现。UI 确实会按需生成——但生成的每张卡片都必须过得了账：无宿主不得入账，无来源链不得登记。

---

## 三、新范式的三个支点

把八条重读收拢，OIS 设计之所以构成范式而非功能，因为它同时替换了旧软件的三个地基：

| 支点 | 旧范式 | 新范式（OIS） | 理论锚点 |
|---|---|---|---|
| **生效的边界** | 代码合并即生效（merge = done） | 具名柜台：未入账，不生效（P3） | 第一轮：Harness 的新边界 |
| **系统的最小单位** | 代码行／函数／任务／消息 | Gap 原语：Primitive · Host · Handle 三位一体 | 第六轮：对象模型 |
| **动力的来源** | 需求文档、KPI、deadline | 动力序：定价先于签署，复利可见即燃料 | 第三轮：两步走；第四轮：呈验面 |

三者咬合为一个闭环：**呈验面生成已定价的 Gap（动力发生）→ 柜台完成具名签付（生效结算）→ 年轮区使复利可见（动力回环）**。缺第一环，柜台无顾客（签署通缩）；缺第二环，定价无结算（金融泡沫）；缺第三环，运动变循环（空转）。

---

## 四、范式判词

> **旧时代的 Software 把人放在系统之外当「用户」；新范式把人放在系统之内当「签付者」。**
>
> **旧时代的 Coding 回答「怎么把代码写出来」；新范式的 Coding 回答「到底该让机器做什么，以及怎么判断它做对了」——并且要求每一次「判断对了」都可归属、不可撤销、可追责。**

Ball 清单的八条里，七条被 OIS 吸收并精化（Review→柜台、做软件→动力序、Bug→落差认领、工具消融→可解耦外壳、分工→具名三判据、好代码→TASTE 双层、UI→对象化卡片），一条被正面反驳（Token 门槛→汗腺批判）。吸收率与反驳点的分布本身即范式更替的签名：**新范式不否认旧器官的死亡，它追问的是这些器官生前到底在做什么——然后为那件事建造真正的设施。**

---

## 五、落地：v0.2 是新范式的第一个可实例化切片

范式若不能实例化，就还只是话语。OIS 的落地路径已经具体到界面规格：

- **《OIS 可实例化界面设计 v0.2》**：五条公理（A1 卡片即对象／A2 Handle 流动宿主不流动／A3 认领比裁决轻／A4 登记不入账／A5 可呈验不得虚构）、三池视图（落差池·待决区·年轮区）、呈验面（登记＋定价＋novel 槽配额＋冷启动）、柜台（mint 时刻=入账时刻=宿主化时刻）、Handle 三种流动（贴现·引用·通兑）、数据契约（Gap 对象＋MismatchDraft 出生证明）。
- **MVP 路线**：P0 三池＋呈验面最小件＋柜台认领；P1 引用边＋层理视图＋治理仪表；P2 通兑＋桌面外壳。
- **可检验性**：ξ 与 r 从第一天被记录；σ 的类型排序是首个可证伪假设。

---

## 六、逐项研究的入口清单（下一步）

范式总结至此收束，接下来进入核心细节的逐项研究。建议的研究次序（每项对应既有设计中的一个承重件）：

1. **落差登记与来源链**（A5 的工程形态：证据链的格式、核查成本、虚警率核算）
2. **呈验定价机制**（E[V]/t/r 默认值的来源、改写留痕、PV 弱表达的诱导风险）
3. **柜台签署与 stake 结构**（stake 三元组的类型学：不同领域什么可被押上）
4. **Handle 注册与跨账房解析**（`oi:` 方案、commit SHA 绑定、ξ 的工程形态）
5. **状态机与退单回路**（倒计时、退单呈验、坏账隔离的完整边界条件）
6. **仪表与防伪**（ρ/σ/ξ/c(s) 的古德哈特防护）

每一项都是新范式的一块承重墙；任何一项落空，范式退回话语。

---

**承袭**：《具名智能前端研究合订本（第一辑）》｜ **现行规格**：《OIS 可实例化界面设计 v0.2》｜ **下一步**：核心细节逐项研究（入口清单见 §六）
