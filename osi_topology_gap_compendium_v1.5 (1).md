# 拓扑边界Gap原语体系升级合订本

**版本**：v1.5
**日期**：2026-09-26
**性质**：原语体系升级合订本。将 R1（对齐研究）与 R2（进阶研究）合并为单一可交付物；v1.1 增补事件类型学；v1.2 增补 Gaph 图论视角；v1.3 增补 Stake 双向原语闭环与三层架构；v1.4.1 修订：删除案例，保留体系哲学与Claim驱动自动废弃机制；v1.5 新增：麦基《故事》Gap原理的语义拓扑解构——Expectation-Event-Conflict-Incentive四元核心结构。一次性具名签付生效。
**签付者**：wend4336
**研究分工**：R1 由 Kimi 2.8 完成（对齐映射）；R2 由 Kimi 3 完成（工程深挖与张力批判）；v1.1 增补由 Kimi 3 完成（事件类型学）；v1.2 增补由 Kimi 3 完成（Gaph 图论视角）；v1.3 增补由 Kimi 3 完成（Stake 双向原语与三层架构）；v1.4.1 修订由 Kimi 3 完成（哲学、自动废弃机制）；v1.5 新增由 Kimi 3 完成（麦基四元语义拓扑解构）。
**升级核心**：Gap 从差值（Δ）升级为拓扑边界（∂）；Stake 从风险覆盖/结构刚度升级为双向签注承诺成本；Handle 从值引用升级为边界状态封装；事件类型学；Gaph 图论视角；三层架构；体系终极设计哲学；Claim 驱动旧签注自动废弃机制；**新增：麦基《故事》Gap原理的语义拓扑解构——E-Ev-C-I四元核心结构，为自动废弃机制提供唯一前置理论基础**。
**承袭**：全部既有研究（合订本模块一至六、OIS v0.2、新软件、三分法、签注语义、Matt Pocock补证、osi_law_F1约束表、osi_harness_insight洞察）

---

## 合订本序章

### 阅读导引

本文档由两轮研究合并而成，各有不可替代的分工：

| 卷次 | 来源 | 内容 | 回答的问题 |
|---|---|---|---|
| 卷一–卷三 | R1 | 原语升级、算子重构、消融分类学 | 升级了什么？与既有体系如何对应？ |
| 卷四 | R1+R2 | 全上下文对齐、体系张力批判 | 既有体系哪里需要改？深层冲突在哪？ |
| 卷五 | R2 | P_pen工程量化、边界类型学、前端升级、P0/P1/P2规格 | 怎么工程化？怎么开工？ |
| 卷六 | R2 | 五项张力裁定 | 升级与既有体系之间的结构冲突如何解决？ |
| 卷七 | COMPARE | 方法论对比与联合使用纪律 | 两轮研究是什么关系？后续怎么用？ |
| 卷八 | R1+R2 | 合并后的开放问题 | 哪些还没解决？ |
| **卷九** | **v1.1 增补** | **事件类型学：拓扑事件与普通事件** | **什么触发了域切换？什么只是域内波动？** |
| **卷十** | **v1.2 增补** | **Gaph 拓扑与 Graph 数据结构** | **拓扑事件如何用图论语言精确表述？** |
| **卷十一** | **v1.3 增补** | **Stake 双向原语与三层架构** | **Stake 的本质是什么？三层如何闭环？** |
| **卷十二** | **v1.4.1 修订** | **体系哲学与自动废弃机制** | **理论如何在现实中落地？旧签注如何自动废弃？** |
| **卷十三** | **v1.5 新增** | **麦基Gap原理的语义拓扑解构** | **Gap的底层语义拓扑结构是什么？四元如何闭环？** |

**核心公式（全文锚点）**：

```
Gap 本体：    G = ∂(S₁, S₂)          // 分立双域的结构性边界
差值度量：    Δ = measure(G)         // 边界的派生量化值（辅助参数）
Gap 收敛：    G = ∅                   // 边界消融，双域合并为单一域
旧算子（废弃）：V_judge = f(Δ)
新算子（生效）：V_judge = f(G_state, P_pen, Stake_real, Threshold)
P_pen 量化：   P_pen(G,t) = P_base(G) × f_stake × f_time × f_network × f_evidence
```

---

## 卷一：原语体系升级详述

### 1.1 Gap：从差值到拓扑边界

**旧定义的问题**：将派生度量值（Δ）等同于本体。在旧范式中，"预期X，实际观测到Y"的落差被默认为 Gap 本身——Gap 的大小由 Δ 决定，Gap 的存在以 Δ≠0 为前提。

**新定义**：Gap 是分隔两个独立状态域 S₁（当下实存态）与 S₂（未来预期/参照态）的拓扑边界 ∂(S₁,S₂)，是使双状态域分立的核心前提，属于结构性、本体性存在。Δ 只是边界上的派生标量测度。

**核心特性**：

1. **域分立优先**：先有边界 G，后有量化 Δ。边界是结构前提，差值是观测结果。
2. **边界独立性**：Δ=0 不代表 G 消失。两侧数值近乎一致，只要状态域分立关系存续，Gap 本体依然存在。
3. **收敛唯一性**：仅边界撤销为收敛，数值归零不属于收敛。

**对既有理论的对齐**：

- **OIS v0.2 公理 A4**："落差"升级为"未宿主 Gap 边界"。登记的是边界存在性声明，不是差值。
- **新软件 §2.3**：Bug 不是"代码与意图的落差"，而是"预期状态域 S₂ 与世界回答状态域 S₁ 之间的拓扑边界 ∂ 被错误穿透"。
- **osi_law_F1 C15**：匿名检测产物不是"落差"，而是"未锚定边界的域分立信号"。只有经 Stake 锚定、具备可裁决结构的边界，才构成 Gap。

### 1.2 Stake：从风险覆盖到双向签注承诺成本（v1.3 核心修正）

**v1.0–v1.2 残留**：此前版本将 Stake 理解为"加固结构、支撑新域"，属于系统层视角。v1.3 将其修正为语义层原语。

**v1.3 最终定义**：

> **Stake = 主体对一组语义断言（Endorsement/Claim）投入的不可撤回承诺成本。**
> 它不是硬件、不是资源、不是单纯的结构约束——**它是语义层面的"签注沉没成本"**。

**双向原语**：

- **S₁-Stake（旧域签注成本）**：主体对旧域核心 Claim/Endorsement 长期背书、叙事固化、身份绑定，导致**撤销旧签注需要付出极高认知、信用、路径、身份成本** → 抵抗拓扑跃迁
- **S₂-Stake（新域签注成本）**：主体为未来新域的新语义断言，提前付出背书成本、认知重构成本、范式切换成本，让新签注体系具备可落地的语义合法性 → 支撑拓扑跃迁

**三层投射关系**：

```
OIS语义层（顶层·本体）
    Stake = 签注承诺的沉没成本
    └── S₁-Stake 抬高旧签注撤销代价
    └── S₂-Stake 前置新签注建立代价
            ↓ 经由 Semantic IR 编译
Semantic IR（中间·翻译层）
    S₁-Stake → 旧图中高权重、不可轻易删除/改写的刚性边约束
    S₂-Stake → 预加载、可激活的潜在新边/新节点约束
            ↓ 投射为
IR拓扑层（底层·现象）
    S₁侧：Gap 边界硬度上升、P_pen 下降 → 系统稳态锁死在 S₁
    S₂侧：预构建新拓扑约束基底 → 保证击穿后的新 S₂ 不会回弹
```

**关键机制——边界可穿透性 P_pen（v1.3 精化）**：

- **P_pen 被压低的唯一本质原因**：不是"结构硬约束"，而是**语义签注撤销成本高昂**——主体极力回避旧语义崩坏，形成顽固语义 Gap 张力。
- **Stake 的核心作用**：不是"覆盖 Δ"也不是"加固结构"，而是**承担语义承诺成本**，经由 Semantic IR 双向编译为 IR 图约束权重，最终表现为拓扑边界的刚度变化。
- **因果剥离**：Δ 的变化是表象波动（普通事件，不触碰核心签注），P_pen 的变化是语义结构变更（拓扑事件，支付签注撤销成本）。

**对既有理论的修正**：

- **OIS v0.2 §六**："stake 起步额"不是"为潜在损失预存保证金"，也不是"提供初始结构刚度"，而是**为语义签注支付前置承诺成本**，为旧域提供撤销阻力、为新域提供建立合法性。
- **osi_law_F1 C18**：名字不可转让，因为**语义签注承诺与特定主体的身份、信用、路径历史深度耦合**，不是单纯的"边界刚度"可以解绑。
- **Matt Pocock 补证**：Spec/Tickets "无 stake"不是"没有风险准备金"，而是**没有语义签注承诺成本**——P_pen = 1 是因为旧签注随时可以零成本撤销、新签注无需前置投入，边界完全可穿透。
- **合订本 v1.2 卷十 Gaph 数据结构**：Stake 的 Graph 作用域需要修正——Stake 不是"为旧 Graph 的节点与边增加支撑约束"，而是**语义签注成本经由 Semantic IR 编译为图边约束权重**；Graph 的"硬度"全部来自上层语义 Stake，而非 Graph 自身。

### 1.3 Handle：从值引用到边界状态封装

**旧定义**：Handle 是"用来访问值实例的可传递入口"，指向存储位置。

**新定义**：Handle 是 Gap 拓扑边界的可寻址、可跨时空传递的符号载体。核心作用不是传递 Δ 差值量化数据，而是封装、引用、流转 **Gap 边界的分立状态、可穿透属性与收敛概率**。

**对既有理论的修正**：

- **三分法**：Value = 状态域内容（S₁ 或 S₂ 的内部状态）；Binding = 状态域命名；Handle = 边界状态封装（∂(S₁,S₂) 的句柄）。
- **OIS v0.2 §七**："跨时间（贴现）"不是"PV 的贴现"，而是"边界收敛概率的时空贴现"；"跨引用（年轮）"不是"值引用的传递"，而是"边界状态与可穿透属性的传递"。
- **osi_law_F1 C8**："可引"不是"值可被引用"，而是"边界状态可被引用、边界可穿透性可被审计"。

### 1.4 货币的本质：边界收敛的时空贴现

**旧定义**：货币是对"差值大小"的定价——E[V] 是收敛预期价值，t 是收敛窗口，r 是贴现率。

**新定义**：货币并非对"数值差值 Δ"的定价，而是对 **未来 Gap 拓扑边界是否消融（收敛）** 的概率性时空贴现。交易与流通的核心标的，是 Gap 边界的存续、穿透、收敛可能性。

**对既有理论的修正**：

- **OIS v0.2 §4.3**：E[V]/t/r 默认值不是"差值的贴现"，而是"边界收敛概率 P_converge(G) 的时空贴现"。呈验价是"边界状态邀请"，不是"差值资产评估"。
- **新软件 §2.2**："值钱的是 E[V] 的判断"修正为"值钱的是对边界收敛概率 P_converge(G) 与边界可穿透性 P_pen 的判断"。

---

## 卷二：CEAA 裁决算子重构

### 2.1 旧算子（废弃）

```
V_judge = f(Δ)          // 仅由差值数值裁决价值
```

病理：把派生度量当成本体，导致"数值波动大 = 价值高""数值趋同 = 价值低"的误判。

### 2.2 新算子（生效）

```
V_judge = f(G_state, P_pen, Stake_real, Threshold)
```

| 参数 | 释义 | 在既有体系中的对应 |
|---|---|---|
| G_state | Gap 拓扑边界存续状态（本体核心） | OIS v0.2 公理 A1（卡片状态即生命周期） |
| P_pen | Gap 边界可穿透性（核心变量） | 新增概念；此前隐含在"stake 覆盖风险"中 |
| Stake_real | Stake 主体实缴的有效边界刚度 | OIS v0.2 柜台"stake 起步额"、osi_law_F1 C3 |
| Threshold | 体系权衡后的裁决阈值 | osi_law_F1 C14（可呈验不得虚构）、A5（来源链必填） |

**关键规则**：Δ 不进入核心裁决算子，仅作为辅助估值参数，不影响裁决结果的真伪判定。

### 2.3 CEAA 各环节重构

| 环节 | 旧逻辑（Δ 优先） | 新逻辑（边界优先） |
|---|---|---|
| **Claim** | 声明"数值差值大小" | 声明"某 S₁ 与 S₂ 存在有效 Gap 边界 G，且该边界具备可穿透/可收敛属性，对应 Stake 损益成立" |
| **Extract** | 提取 Δ 数值与相关证据 | 提取三层：①拓扑边界 G 的存续状态；②锚定边界的 Stake 实缴体量与责任范围；③承载边界引用的 Handle 链路完整性 |
| **Attest** | 核验 Δ 数值是否准确 | 通过阈值 Cutoff 判定 Gap 边界是否为有效边界；通过消融测试剥离次要变量，验证 Stake 锚定真实性、Handle 链路有效性 |
| **Adjudicate** | 判定 Δ 的大小决定是否生效 | 判定 Gap 边界的可穿透性 P_pen 与收敛概率；输出边界状态结论（有效存续/边界消融/边界可穿透/边界闭锁） |

---

## 卷三：消融分类学与医疗方法论

### 3.1 方法论映射

| 维度 | 医疗临床研究 | 消融分类学（新定义） | 既有体系对应 |
|---|---|---|---|
| **原语** | 连续生理指标（血氧、肿瘤指标） | 连续的状态分立关系 = Gap 拓扑边界 | osi_law_F1 C15：Gap 是连续态，结论依赖阈值离散化 |
| **事实判断** | 阈值裁决（Cutoff）将连续信号切割为阳性/阴性 | 阈值裁决机制将边界存续状态转化为有效/无效 | v0.2 §4.2 类型初判：验收/意外/断裂/伪 Gap |
| **消融研究** | 变量剔除、单因素对照，验证单指标贡献度 | 逐一切除子 Gap、撤销 Stake 锚定、切断 Handle 链路，观测裁决结果漂移 | osi_law_F1 C22（防伪协议：引用质量约束、三角验证） |
| **代价权衡** | 误诊/漏诊的风险代价 | Stake 的损益承载 = 边界被错误穿透的代价 | v0.2 §4.6 护栏仪表：虚警率→信任折旧 δ |

### 3.2 核心差异

医疗判定的是**已存在的生理事实**（边界在当前时刻即存在或不存在）；Gap 体系裁决的是**未来边界收敛的概率性事实**（边界在未来可能收敛也可能存续）。这一差异解释了为什么 Gap 体系需要 **时空贴现** 而医疗不需要。

### 3.3 医疗工具迁移（R2 深化）

| 医疗工具 | 医疗用途 | OSI 迁移形态 | 工程落点 |
|---|---|---|---|
| **ROC 曲线** | 评估阈值 Cutoff 的灵敏度/特异度权衡 | 边界裁决阈值优化器 | 基于历史裁决数据自动优化 Threshold 参数 |
| **Kaplan-Meier 生存曲线** | 估算群体随时间的生存概率 | 边界收敛概率曲线 | 按边界类型分组预测 P_converge(G) 随时间变化 |
| **Cox 比例风险模型** | 多变量对生存时间的影响 | 边界穿透风险模型 | 量化各因子对 P_pen 的独立贡献 |
| **NNT** | 治疗多少患者预防一例不良事件 | NSN（Number Needed to Stake） | 需要多少 Stake 增量才能将 P_pen 降低一个单位 |
| **消融研究** | 逐一切除变量验证模型核心组件 | 自动化边界消融测试 | 5 Phase 流水线（基线→Stake消融→证据链消融→Handle消融→综合报告） |

### 3.4 自动化消融测试工程方案

```
Phase 1：基线裁决
        └── 运行完整 CEAA，记录 R_base 与 P_pen 四因子分解
Phase 2：Stake 消融
        └── 模拟 Stake_real→0，重新裁决 R_no_stake，计算漂移度 δ_stake
Phase 3：证据链消融
        └── 逐条移除 evidenceChain[i]，计算单条证据边际贡献 γ_i
Phase 4：Handle 链路消融
        └── 切断 ringRefs[] 各边，计算引用网络效应 η_j
Phase 5：综合报告
        └── 各组件贡献度排序 + 识别关键单一变量 + 加固建议
```

界面落点：Gap 卡片展开态增加「消融测试」按钮，结果以贡献度条形图呈现。

---

## 卷四：全上下文对齐与修正

### 4.1 对 OIS v0.2 的修正

| 条款 | 旧表述 | 修正后表述 |
|---|---|---|
| **公理 A1** | 卡片即对象，卡片状态即生命周期 | 卡片即拓扑边界对象，卡片状态即边界生命周期（存续/悬置/消融） |
| **公理 A4** | 未宿主落差必须登记但不入账 | 未宿主边界必须登记但不入账 |
| **§4.2 登记表单** | 预期—回答结构：我预期 X / 实际观测到 Y | 双状态域分立声明：S₁（当下实存态）/ S₂（未来预期/参照态） |
| **§4.3 定价呈验** | E[V]/t/r → 呈验价 | P_converge(G)/t/r → 边界收敛概率的时空贴现 |
| **§五 Gap 卡片** | PV ¥1.83 · 剩余 47:12 | P_pen=0.23 · 刚度 ¥2.00K · 收敛窗口 47:12 |
| **§六 柜台** | 确认即 mint | 裁决即边界刚度确认 + mint |
| **§七 Handle** | 跨时间（贴现）= PV 追溯 | 跨时间（贴现）= 边界收敛概率追溯 |

### 4.2 对"新软件"八条判断的重读

| 判断 | 拓扑边界升级后的重读 |
|---|---|
| **Code Review 会消失** | 签付的是"边界刚度确认"，不是"差值校验" |
| **Bug 变成你让 AI 做错了** | 边界可穿透性的发现与认领——AI 错误地穿透了 S₁→S₂ 的拓扑边界 |
| **写代码弱化，做软件升值** | 对边界收敛概率与可穿透性的判断 |
| **UI 按需生成** | 承载边界状态对象（∂(S₁,S₂) 的卡片化），不承载差值计算功能 |

### 4.3 对三分法的升级

| 层级 | 旧映射 | 新映射 |
|---|---|---|
| **Value（值）** | 纯粹语义内容 | 状态域内容（S₁ 或 S₂ 的内部状态） |
| **Binding（绑定）** | 名字↔存储位置的映射 | 状态域命名（S₁="当下"、S₂="预期"） |
| **Handle（引用）** | 访问值实例的入口 | 封装边界状态 ∂(S₁,S₂) 的符号载体 |

### 4.4 对 osi_law_F1 的升级

| 约束 | 升级后表述 |
|---|---|
| **C1 总纲三分** | "生效"精化为"边界刚度确认（降低 P_pen）必须具名" |
| **C15 匿名的 Gap** | 未锚定 Stake 的边界不是 Gap，是可穿透性为 1 的噪声信号 |
| **C18 非转让核心** | Stake 锚定的边界刚度不可转让；收敛结果可贴现 |
| **C20 柜台五环** | 边界声明→边界核验→刚度确认（Stake）→边界状态入账→边界追溯 |

### 4.5 对 Matt Pocock 补证的升级

| 补证条目 | 拓扑边界升级后的读法 |
|---|---|
| **法：可匿名生产** | Agent 生成的是状态域内容（Value），不生成边界；边界存在性需具名声明 |
| **消融分类学** | 不满足三判据 = 无 Stake 锚定 = P_pen = 1 = 边界完全可穿透 = 无价值 |
| **Handle：临时断点** | 跨上下文传递边界状态封装；任务完成 = 边界已消融或不再相关，Handle 丢弃 |
| **白名单：CONTEXT.md** | 给 LLM 的状态域命名契约，帮助 LLM 理解 S₁/S₂ 的边界，但不提供边界刚度 |

---

## 卷五：进阶工程规格

### 5.1 P_pen 工程量化方案

**多因子衰减函数**：

```
P_pen(G, t) = P_base(G) × f_stake(G, t) × f_time(t) × f_network(G, t) × f_evidence(G)
```

| 因子 | 计算方式 | 初始参数 |
|---|---|---|
| **P_base** | 边界类型学判定（见§5.2） | 刚性 0.1 / 柔性 0.4 / 渗透 0.8 / 虚假 1.0 |
| **f_stake** | `exp(-α × Stake_real / Stake_threshold)` | α = 1.0 |
| **f_time** | `1 - exp(-β × t_elapsed / t_window)` | β 与领域类型耦合 |
| **f_network** | `1 / (1 + γ × citation_depth)` | γ = 0.5 |
| **f_evidence** | `evidence_count / (evidence_count + δ)` | δ = 3 |

**界面呈现规格**：

```
P_pen=0.23 · 刚度 ¥2.00K · 收敛窗口 47:12
      └── 因子分解：Stake×0.71 | 时间×0.85 | 网络×0.95 | 证据×0.98
```

**动态事件响应（区分事件类型）**：

| 事件 | 事件类型 | P_pen 影响 | 域结构变化 |
|---|---|---|---|
| 域内状态数值波动（Δ 涨跌） | 普通事件 | 无 | 无——始终在原有域内 |
| Stake 追加 | 拓扑事件 | ↓ 下降 | 边界刚度增加 |
| Stake 赎回/失效 | 拓扑事件 | ↑ 上升 | 边界锚定松动 |
| 新证据链补充 | 拓扑事件 | ↓ 下降 | 边界支撑增强 |
| 证据链被证伪 | 拓扑事件 | ↑ 上升 | 边界支撑削弱 |
| 引用边增加（质量约束后） | 拓扑事件 | ↓ 下降 | 网络支撑增强 |
| 时间超窗 | 拓扑事件 | ↑ 上升 | 边界时效衰减 |
| 世界回答抵达 | 拓扑事件 | 重置 | G=∅（边界消融）或重新锚定 |
| 边界被外部行为击穿 | 拓扑事件 | P_pen→1 | 跨域跃迁，生成新拓扑域 |

### 5.2 边界类型学四分类

| 类型 | 结构特征 | P_base | 典型场景 |
|---|---|---|---|
| **刚性边界** | 高 Stake 密度、多证据链、历史引用丰富 | 0.0–0.2 | 已验证架构决策、已签署合同条款 |
| **柔性边界** | 中等 Stake、证据链待补充、引用网络形成中 | 0.2–0.6 | 待验证技术假设、进行中的需求对齐 |
| **渗透边界** | 低 Stake、证据链薄弱、高外部依赖性 | 0.6–0.9 | 第三方依赖风险、未验证市场假设 |
| **虚假边界** | 无有效 Stake、证据链虚构或断裂 | 0.9–1.0 | 伪 Gap、幻觉产生的虚假落差 |

**判定流程**：证据链结构分析 → Stake 锚定检测 → 伪边界过滤（A5 强制） → P_base 赋值 → P_pen 动态计算。

### 5.3 前端/界面设计升级

**卡片规格升级**：

```
旧主视觉：落差内容（预期 X / 实际 Y）
新主视觉：边界声明（S₁ ──∂── S₂）+ 边界类型标签 + P_pen 仪表盘

旧资产面：PV ¥1.83 · 剩余 47:12
新资产面：P_pen=0.23 · 刚度 ¥2.00K · 收敛窗口 47:12
```

**三池视图升级**：

| 池 | 新定义 | 关键动作 |
|---|---|---|
| **① 落差池** | 未宿主边界声明（P_pen = 1） | 登记 = 边界存在性声明；认领 = Stake 注入降低 P_pen |
| **② 待决区** | 已锚定边界·悬置（P_pen < 1） | 签付确认 = 边界刚度确认；退单 = Stake 撤回 |
| **③ 年轮区** | 已收敛/已消融（G = ∅） | 边界消融 = 双域合并；可引用/追溯 |

**呈验面升级**：回答"值得你确认哪条边界的刚度？"

**柜台升级**：边界摘要 → 刚度确认（Stake 输入）→ 可穿透性判定 → 不可逆提示 → mint。

### 5.4 P0/P1/P2 分级规格

**P0（MVP 必做）**：

| # | 升级项 | 验收标准 |
|---|---|---|
| 1 | P_pen 字段 | Gap 对象可通过 API 读取 P_pen 及四因子分解 |
| 2 | 边界类型标签 | 登记时自动判定，用户可申诉/修正 |
| 3 | Δ 降级呈现 | 用户首屏看不到 Δ 数值，必须先看到 P_pen |
| 4 | Stake 刚度反馈 | 认领/追加 Stake 后实时显示 P_pen 变化动画 |

**P1（治理件）**：P_pen 趋势图、消融测试自动化、ROC 阈值优化器、Kaplan-Meier 边界收敛曲线。

**P2（扩展）**：跨账房 P_pen 重算、NSN 计算器、动力序公式 BoundaryRent 模型重写。

---

## 卷六：体系张力批判与裁定

### 6.1 张力一：Δ 的认知惯性与界面诚实

**问题**：即使工程上把 Δ 降级为辅助参数，"预期—回答"的呈现方式本身就在强化 Δ 思维。

**裁定**：
1. "预期—回答"结构从卡片主视觉降级为可展开的次级信息
2. 卡片主视觉改为"S₁ ──∂── S₂"边界图示
3. Δ 数值用灰色小字呈现，标注"参考值·非裁决依据"

### 6.2 张力二：Stake 金融隐喻冲突

**问题**：动力序公式 PV = E[V]/(1+r)^t 建立在数值贴现隐喻上，与边界状态本体冲突。

**裁定**：
```
旧：PV = E[V] / (1+r)^t
新：PV = E[BoundaryRent(G)] × P_converge(G,t) / (1+r)^t
```
BoundaryRent(G) = 边界存续期间避免的穿透损失。Stake 的回报不是"利息"，而是"边界刚度溢价"。

### 6.3 张力三：边界消融与坏账的区分

**问题**：拓扑边界框架引入了"消融"（G=∅ 但无世界回答）作为第三种结束态。

**裁定**：

| 结束态 | 边界状态 | 处理路径 |
|---|---|---|
| **收敛** | G=∅，有世界回答背书 | 沉积为年轮 |
| **消融** | G=∅，无明确世界回答 | Stake 仍在→主动放弃预期；Stake 失效→归入坏账 |
| **坏账** | G 存在但 Stake 失效，P_pen→1 | 隔离标记，只读，不入账 |
| **闭锁** | G 存在且 P_pen≈0，长期无世界回答 | 触发退单倒计时或强制收敛 |

### 6.4 张力四：Handle 流动性悖论

**问题**：Handle 跨上下文传递时，P_pen 应该重算，不是原样复制。

**裁定**：
1. Handle 传递时，目标上下文基于本地四因子重新计算 P_pen
2. 引用边不是"复制边界状态"，而是"增加一条网络支撑"
3. 跨账房 Handle 解析必须携带边界类型初始参数，由目标账房重新评估

### 6.5 张力五：事件误分类（v1.1 增补）

**问题**：系统若不能区分普通事件（域内 Δ 波动）与拓扑事件（边界穿透），将导致两类病理：
- **虚警**：把普通事件误判为拓扑事件 → 不必要的 Stake 消耗、边界刚度反复调整、系统震荡
- **漏警**：把拓扑事件误判为普通事件 → 边界被击穿而未感知、P_pen 已跃迁但未触发裁决、系统结构性风险累积

**裁定**：
1. **事件类型判定器为 P0 必做项**（见卷九 §9.4）
2. **误判率纳入护栏仪表**：虚警率（普通事件误判比例）与漏警率（拓扑事件误判比例）与信任折旧 δ 联动
3. **事件流可视化**：Gap 卡片增加时间轴，区分标注普通事件（灰色·Δ 波动）与拓扑事件（彩色·P_pen 变化），让误判可被人工审计
4. **消融测试扩展**：Phase 流水线增加"事件类型消融"——将入站事件逐一重新分类，观测裁决结果对事件类型判定的敏感度

---

## 卷七：方法论与对比研究

### 7.1 R1 与 R2 的分工

| 维度 | R1（对齐） | R2（进阶） |
|---|---|---|
| 研究目标 | 把升级映射到既有体系 | 工程深挖 + 盲区批判 |
| 核心产出 | 修正清单 + 开放问题 | 可执行方案 + 原型规格 |
| 方法论风格 | 忠实转译、逐条对照 | 独立推演、工具迁移、批判裁定 |
| 数学化程度 | 概念级公式 | 可计算模型 + 参数标定 |
| 工程落点 | 界面概念描述 | P0/P1/P2 分级规格、验收标准 |

### 7.2 联合使用纪律

> 任何重大理论升级必须先过 R1（对齐），再过 R2（深挖）。R1 保证不丢承袭，R2 保证能落地。二者缺一，升级退回话语。

- **R1 适用场景**：新理论首次进入体系；需要建立承袭链、维护术语一致性；需要向既有读者证明兼容性。
- **R2 适用场景**：对齐完成后需要工程落地；需要识别体系张力、防止结构性 bug；需要给出可执行规格。

---

## 卷九：事件类型学——拓扑事件与普通事件（v1.1 增补）

> 承《S1、S2拓扑域与拓扑事件、普通事件完整释义》（2026-09-25 上传）。本节将事件类型学纳入合订本，作为拓扑边界原语体系的必要补充。

### 9.1 核心区分

在分立双域拓扑体系中，系统被边界 Gap 划分为两个独立拓扑域 S₁ 与 S₂。**两类事件作用于系统的层级、效果完全不同**：

| 对比维度 | 普通事件（状态事件） | 拓扑事件（边界穿透事件） |
|---|---|---|
| **作用对象** | S₁ 或 S₂ 单一域内部状态变量 | S₁ 与 S₂ 之间的 Gap 拓扑边界 |
| **核心影响** | 仅改变状态差值 Δ，产生表象波动 | 改变边界可穿透性 P_pen，完成拓扑结构变更 |
| **域结构变化** | 拓扑结构、域划分完全不变 | 打破旧域结构，生成收敛旧问题的全新拓扑域 |
| **系统归属** | 始终停留在原有拓扑域内 | 实现跨域跃迁，完成系统拓扑迭代 |
| **事件特征** | 连续渐变、可反复震荡、可逆 | 阈值突变、离散跃迁、结构性不可逆 |

### 9.2 普通事件：域内状态波动

普通事件仅作用于 S₁ 或 S₂ 单一拓扑域内部，只修改系统内部的状态数值，不会触碰、击穿 S₁ 与 S₂ 之间的 Gap 拓扑边界。

- **核心影响**：仅为状态差值 Δ 的起伏波动，属于纯粹的表象变化。
- **域归属**：事件发生后，系统的拓扑域归属不会改变，始终停留在原有域内。
- **边界稳定性**：域的结构属性、边界的可穿透性 P_pen 均保持稳定。
- **特征**：连续、渐变、可震荡，数值可反复涨跌，但无法实现跨域跃迁，不会催生新的系统结构。

**工程意义**：系统必须能够识别并过滤普通事件——大量的 Δ 波动不应触发边界状态变更或 Stake 调整。若将普通事件误判为拓扑事件，会导致不必要的边界刚度消耗与 Stake 浪费。

### 9.3 拓扑事件：边界穿透与域切换

拓扑事件是直接作用于 S₁ 与 S₂ 之间 Gap 拓扑边界的结构性事件，也是 P_pen 唯一度量的核心事件。

- **边界击穿本质**：绝非对旧域的简单复制、平移或数值替换，而是**全新拓扑域的生成**。新形成的拓扑域会针对性收敛旧域存续过程中积累的漏洞、矛盾与各类问题，完成对旧域拓扑结构的彻底改写与迭代。
- **核心变量**：P_pen 的结构性改变才是域切换的核心本质；Δ 的数值变化只是更迭的附属表象。
- **特征**：离散突变、阈值触发。一旦完成，系统将彻底从旧域跃迁到新生拓扑域，实现系统层级的拓扑升级与结构重构。

**Stake 的适配作用**：Stake 的核心价值仅作用于拓扑结构层面，专门加固 Gap 边界、降低 P_pen，抑制拓扑事件的发生，阻止旧域拓扑结构被改写。但 Stake **不干预域内状态运行**，对普通事件引发的 Δ 数值表象波动无任何作用——完美契合「Δ 为表象、P_pen 为本质」的因果剥离逻辑。

### 9.4 事件路由的工程规格

**P0 增补项**：

| # | 升级项 | 验收标准 |
|---|---|---|
| 5 | **事件类型判定器** | 系统能够自动区分入站事件为普通事件或拓扑事件；误判率 < 5% |

**判定逻辑（初版）**：

```
输入：入站事件 E

步骤1：作用对象检测
        └── E 是否直接修改 Gap 边界的属性（P_pen、Stake 锚定、证据链、引用网络）？
            └── 是 → 拓扑事件 → 进入步骤2
            └── 否 → 普通事件 → 记录 Δ 变化，不触发边界状态更新

步骤2：拓扑事件子类型判定
        └── E 修改的是哪类边界属性？
            ├── Stake 相关（追加/赎回/失效）→ 刚度变更事件
            ├── 证据链相关（补充/证伪）→ 支撑变更事件
            ├── 引用网络相关（增加/删除）→ 网络变更事件
            ├── 时间相关（超窗/到期）→ 时效变更事件
            └── 世界回答抵达 → 收敛事件（G=∅ 或重新锚定）

步骤3：边界状态更新
        └── 根据事件子类型，调用 P_pen 多因子模型重新计算
        └── 触发边界生命周期状态机转换
        └── 若 P_pen 跨越阈值（如从 <0.5 跃迁至 >0.8），触发拓扑跃迁警报
```

**界面落点**：Gap 卡片增加「事件流」时间轴，区分标注普通事件（灰色·Δ 波动）与拓扑事件（红色/绿色·P_pen 变化）。

### 9.5 与既有体系的对齐

| 既有概念 | 事件类型学映射 |
|---|---|
| **新软件 §2.3：Bug** | Bug = 错误的拓扑事件——AI 错误地穿透了不应穿透的边界 |
| **新软件 §2.2：做软件升值** | 判断的是拓扑事件的发生概率与边界收敛时机 |
| **OIS v0.2 §4.6：护栏仪表** | 虚警率 = 普通事件被误判为拓扑事件的比例；漏警率 = 拓扑事件被误判为普通事件的比例 |
| **osi_law_F1 C7：三阶蒸发** | 一阶蒸发 = 普通事件未抵达感知；二阶/三阶蒸发 = 拓扑事件未获裁决 |
| **osi_harness_insight 洞察 6：状态机语义** | 状态机必须区分"域内状态（普通事件驱动）"与"边界状态（拓扑事件驱动）"两套转换图 |

---

## 卷八：开放问题与下一步

### 8.1 合并后的开放问题（已去重）

| # | 问题 | 状态 | 优先级 |
|---|---|---|---|
| 1 | **P_pen 参数标定**：α/β/γ/δ 需要历史数据拟合，建议先做模拟数据验证 | 待实验 | P0 |
| 2 | **动力序公式重写**：BoundaryRent(G) 的精确定义与工程实现 | 待裁定 | P1 |
| 3 | **消融测试自动化**：5 Phase 流水线的工程成本与性能影响 | 待评估 | P1 |
| 4 | **边界类型学验证**：四分类的判定准确率需要对照实验 | 待实验 | P0 |
| 5 | **Handle 跨账房 P_pen 重算**：跨账房解析协议的增量规格 | 待设计 | P2 |
| 6 | **首签担保与边界类型**：新类型域无年轮时，首签推荐卡的"值得锚定"由谁担保 | 继承自 v0.2 §十一.5 | P1 |
| 7 | **事件类型判定器**：普通事件与拓扑事件的自动区分算法及误判率验证 | 待实验 | P0 |
| 8 | **事件流可视化**：Gap 卡片时间轴的交互规格与性能影响 | 待设计 | P1 |

### 8.2 下一步行动

1. **签付本合订本**：由 wend4336 具名签付，生效为 v0.3 规格的理论基线。
2. **启动 P0 四项**：P_pen 字段、边界类型标签、Δ 降级呈现、Stake 刚度反馈。
3. **模拟数据验证**：用合成数据验证 P_pen 多因子模型的预测准确性。
4. **边界类型学对照实验**：人工标注一批 Gap 的边界类型，与自动判定结果比对。
5. **将本合订本并入《具名智能前端研究合订本》第二辑**。

---

## 卷十：Gaph 拓扑与 Graph 数据结构（v1.2 增补）

> 承《Gaph 拓扑与 Graph 边节点关系》（2026-09-25 上传）。本节将图论视角纳入合订本，为拓扑边界原语体系提供可计算的数据结构基线。

### 10.1 核心映射：拓扑边界 ↔ Graph 结构

从图（Graph）视角重新表述拓扑事件：

| 拓扑语言 | Graph 语言 | 工程含义 |
|---|---|---|
| **S₁（当下实存态）** | **旧 Graph** G₁ = (V₁, E₁, w₁) | 节点集合 V₁、边集合 E₁、权重函数 w₁ 构成的稳态图 |
| **S₂（未来预期/参照态）** | **新 Graph** G₂ = (V₂, E₂, w₂) | 预期收敛后的目标图结构 |
| **Gap 边界 ∂(S₁,S₂)** | **Gaph 分隔** = G₁ 与 G₂ 之间的结构差异 | 不是权重差异，而是节点/边/连通性的结构差异 |
| **普通事件** | **边权重修改** Δw(e) | 仅修改 w(e)，V 与 E 的集合不变，Graph 拓扑不变 |
| **拓扑事件** | **Graph 结构重构** G₁ → G₂ | 节点集合、边的连接规则、连通性发生改变，生成全新 Graph |
| **P_pen** | **Graph 重构概率** P(G₁→G₂) | 原有关系边失效、触发 Graph 重构的概率与可行性 |
| **Stake** | **节点与边的支撑约束** c(v), c(e) | 为旧 Graph 的实体与关系边增加约束，阻止 Graph 重构 |
| **边界消融（G=∅）** | **Graph 合并** G₁ ∪ G₂ = G' | 双域合并为单一连通图 |

### 10.2 普通事件 = 边权重修改（Graph 拓扑不变）

普通事件仅修改 Graph 节点与边上的权重数值：

- **节点集合 V 不变**：没有新节点加入，没有旧节点移除
- **边集合 E 不变**：没有新边建立，没有旧边断裂
- **连通性不变**：图的连通分量数量与结构不变
- **仅权重函数 w 变化**：w'(e) = w(e) + δ，产生 Δ 表象波动

```
普通事件前：G = (V, E, w)     普通事件后：G' = (V, E, w')
                              where w'(e) = w(e) + Δw(e)
```

**工程判据**：若入站事件仅改变权重函数而不触及 V、E、连通性，则判定为普通事件，不触发边界状态更新。

### 10.3 拓扑事件 = Graph 结构重构（拓扑改变）

拓扑事件是 Graph 的结构性重构：

- **节点集合变化**：V₁ ≠ V₂（新节点加入或旧节点移除）
- **边集合变化**：E₁ ≠ E₂（新边建立或旧边断裂）
- **连通性变化**：连通分量数量、结构、直径发生改变
- **权重函数重构**：w₂ 不是 w₁ 的线性变换，而是基于新结构的全新权重分配

```
拓扑事件前：G₁ = (V₁, E₁, w₁)
拓扑事件后：G₂ = (V₂, E₂, w₂)
               where V₁ ≠ V₂ 或 E₁ ≠ E₂（结构性差异）
```

**关键判词**：拓扑事件"不是给旧图换一组权重，而是重构关系结构"。新 Graph 收敛旧 Graph 中原有关系无法承载的内在矛盾。

### 10.4 Stake 的 Graph 作用域

Stake 在 Graph 视角下的精确作用：

- **作用对象**：旧 Graph G₁ 的节点 v ∈ V₁ 与边 e ∈ E₁
- **作用机制**：为节点与边增加支撑约束 c(v)、c(e)，提高其存续阈值
- **效果**：降低 Graph 重构概率 P(G₁→G₂)，即降低 P_pen
- **不作用域**：不抑制节点/边权重的小幅波动（不干预 Δw）

```
Stake 注入前：G₁ = (V₁, E₁, w₁)          Stake 注入后：G₁' = (V₁, E₁, w₁, c)
                                                            c(v), c(e) ↑
                                                            P(G₁→G₂) ↓
```

### 10.5 麦基 Gap 的 Graph 同构

麦基 Gap 原理在 Graph 视角下的精确表述：

> 主体所持有的原有关系图（预期模型）G_expect = (V_exp, E_exp, w_exp) 与现实观测图 G_obs = (V_obs, E_obs, w_obs) 之间产生不可弥合的缺口——即 G_expect 与 G_obs 的结构性差异（不是权重差异）。原有连接假设失效，必须重建关系模型，生成 G_new。

这与模块四 §2.1 的麦基原理严格同构，但将"叙事动作"翻译为"Graph 结构变更"：

| 麦基叙事学 | Graph 拓扑学 |
|---|---|
| 角色采取行动 | 主体在 Graph 中发起状态转移 |
| 期待结果 X | 预期 Graph G_expect |
| 现实回答 ≠X | 观测 Graph G_obs ≠ G_expect（结构性差异） |
| Gap（落差） | ∂(G_expect, G_obs) = Graph 结构差异 |
| 新动作打开新 Gap | 生成 G_new，其结构与 G_expect/G_obs 均不同 |

### 10.6 Gaph 数据结构规格（工程基线）

基于图论视角，拓扑边界系统的核心数据结构可规范为：

```typescript
// Gaph 拓扑空间
interface GaphTopology {
  layer: string;              // "Gaph-Gap-拓扑空间"
  nodes: Map<string, DomainNode>;   // 域节点 S₁, S₂, ...
  edges: Array<GaphEdge>;     // 边界边 ∂
  gapSet: Array<GapBoundary>; // 边界集合
  stakePool: Array<StakeAnchor>; // 锚定池
  handleRegistry: Map<string, string>; // 句柄注册表
}

interface DomainNode {
  domain: string;             // 域标识："S₁当下" / "S₂预期"
  state: number;              // 域内状态值（可波动）
  graph: Graph;               // 该域的 Graph 结构
}

interface GaphEdge {
  source: string;             // 源域节点 ID
  target: string;             // 目标域节点 ID
  weight: number;             // 边界刚度权重（Stake 注入后）
  type: "topology" | "ordinary"; // 边类型：拓扑边 / 普通边
  p_pen: number;              // 边界可穿透性 [0,1]
}

interface GapBoundary {
  source: string;
  target: string;
  p_pen: number;              // 可穿透性
  p_base: number;             // 基础可穿透性（边界类型）
  stake_real: number;         // 实缴刚度
  gap_state: "declared" | "suspended" | "absorbed" | "ruptured" | "forged";
}
```

**关键设计约束**：
1. **普通边**（type="ordinary"）的修改只改变 weight，不改变 source/target；系统判定为普通事件
2. **拓扑边**（type="topology"）的修改若触及 source/target 或改变 gap_state，系统判定为拓扑事件
3. **P_pen 重算**：任何拓扑边修改后，基于四因子模型重新计算 p_pen
4. **Graph 重构检测**：比较修改前后的 nodes/edges 集合，若集合不同则触发拓扑事件路由

### 10.7 与既有体系的对齐

| 既有概念 | Gaph 图论映射 |
|---|---|
| **OIS v0.2 公理 A1（卡片即对象）** | GaphTopology 中的 GapBoundary 对象 |
| **OIS v0.2 §4.2 登记表单** | 声明一个新的 GaphEdge（source=S₁, target=S₂） |
| **OIS v0.2 §六 柜台 mint** | 将 GapBoundary.gap_state 从 "declared" 转为 "suspended"（P_pen < 1） |
| **osi_law_F1 C20 柜台五环** | 边界声明→边界核验→刚度确认→状态入账→边界追溯 = GapBoundary 生命周期 |
| **osi_harness_insight 洞察 3（对象一等主界面）** | GaphTopology 为系统的一等数据结构，所有界面操作映射为 Graph 操作 |
| **Matt Pocock 补证：Spec/Tickets** | Spec = 对 GaphEdge 的临时声明（无 Stake，P_pen=1）；Tickets = 跨会话的 GaphEdge Handle |

---

## 卷十一：Stake 双向原语与三层架构（v1.3 增补）

> 承《Stake 双向原语闭环（OIS语义层 + Semantic IR + IR拓扑层）最终定稿》（2026-09-25 上传）。本节将 Stake 的本质从系统层"结构刚度"修正为语义层"签注承诺成本"，建立三层闭环架构。

### 11.1 核心修正：Stake 的本质是签注承诺成本

**此前版本的缺陷**：v1.0–v1.2 将 Stake 理解为"加固结构、支撑新域"，属于 IR 拓扑层的系统视角，没有回归 OIS 语义原语本质。

**v1.3 最终定义**：

> **Stake = 主体对一组语义断言（Endorsement/Claim）投入的不可撤回承诺成本。**

它不是硬件、不是资源、不是结构约束——**它是语义层面的"签注沉没成本"**。

由此导出唯一正确的双向机制：

- **S₁-Stake（旧域签注成本）**：维持旧语义体系的存续成本，**撤销旧签注需要极高代价** → 抵抗拓扑跃迁
- **S₂-Stake（新域签注成本）**：建立新语义体系的背书成本，**新建新签注需要前置投入代价** → 支撑拓扑跃迁

**所有 IR 拓扑结构变化，全部是语义签注成本的下层投射。**

### 11.2 三层架构

#### 第一层：签注语义层（OIS 原语层·最顶层）

Stake 的核心定义在此层，是 OIS 专属原语：

**Stake 是签注的承诺沉没成本。**

- **S₁-Stake**：主体对旧域核心 Claim/Endorsement 长期背书、叙事固化、身份绑定，导致撤销旧签注需要付出极高认知、信用、路径、身份成本。这是系统不肯跃迁、P_pen 被压低的**唯一本质原因**（不是结构硬约束，是语义代价）。
- **S₂-Stake**：主体为未来新域的新语义断言，提前付出背书成本、认知重构成本、范式切换成本，让新签注体系具备可落地的语义合法性。

#### 第二层：Semantic IR 中间桥接层（语义→结构翻译层）

Semantic IR 的作用：**把「语义签注成本」编译为「图边约束权重」**。

- S₁-Stake（语义撤销成本高）→ 转化为 IR 图中**不可轻易删除、不可轻易改写的高权重关系边约束**
- S₂-Stake（语义建立成本前置）→ 转化为 IR 图中**预加载、可激活的潜在新边、新节点约束**

**结论**：IR 图重构的门槛，本质是语义签注成本的结构化映射。

#### 第三层：IR 拓扑层（系统结构层·最下层）

IR 层无语义、无认知，只有拓扑结果：

- **S₁ 侧**：加固 S₁/S₂ 拓扑 Gap 边界，压制边界可穿透性 P_pen，维持旧拓扑稳态
- **S₂ 侧**：预构建新拓扑约束基底，保证击穿后的新 S₂ 拓扑不会回弹、不会伪跃迁

**IR 层的所有结构硬度，全部来自上层语义 Stake，而非自身。**

### 11.3 唯一正确的因果链条

```
语义层（OIS 本体）：
    签注存在高额撤销成本（S₁-Stake）
        → 主体极力回避旧语义崩坏
        → 形成顽固语义 Gap 张力

Semantic IR（翻译传导）：
    语义高成本 → 编译为 IR 高刚性边约束 → 提高图重构门槛

IR 拓扑层（现象结果）：
    Gap 边界硬度上升、P_pen 下降 → 系统稳态锁死在 S₁

——————————————

当现实冲突持续累积，语义 Gap 无法被 Δ 微调吸收：

    S₂-Stake（新签注前置成本）到位 → 新语义体系具备背书合法性
        → Semantic IR 加载新约束 → P_pen 突破阈值
        → 拓扑事件击穿边界 → 旧签注批量失效、撤销（支付高额撤销成本）
        → 新 IR 拓扑生成、新语义签注接管系统
```

### 11.4 关键学术区分（对 v1.0–v1.2 的修正）

**1. 为什么普通事件改不了拓扑？**

普通事件只改变 Δ 数值，**不触碰核心签注，不产生签注撤销成本**，所以 Semantic IR 仅微调权重、不改动边结构，IR 拓扑不变。

**2. 为什么拓扑事件是结构性本质变化？**

拓扑事件 = **主动支付高额 Stake 签注撤销成本**，废弃旧语义体系，重建新语义体系，再下传重构 IR 图。

**3. 为什么 Gap 一定是 OIS 原语？**

因为 Gap 的阻力、动力、跃迁代价，全部来源于**具名智能的语义签注承诺成本**，而非物理系统、不是硬件结构、不是数据波动。**IR 层 Gap 只是投影，OIS 语义层 Gap 才是本体。**

### 11.5 对 v1.2 的修正清单

| v1.2 表述 | v1.3 修正 | 修正理由 |
|---|---|---|
| Stake = "边界结构刚度" | Stake = "语义签注承诺成本" | 结构刚度是 IR 层的现象，不是本体 |
| Stake "加固 Gap 拓扑边界、降低 P_pen" | S₁-Stake 抬高旧签注撤销代价 → 压低 P_pen；S₂-Stake 前置新签注建立代价 → 支撑跃迁 | 单向→双向 |
| "Stake 的 Graph 作用域：为节点与边增加支撑约束 c(v), c(e)" | Stake 经由 Semantic IR 编译为图边约束权重；Graph 的硬度全部来自上层语义 Stake | Graph 自身无硬度 |
| P_pen 被压低是因为"结构硬约束" | P_pen 被压低是因为"语义签注撤销成本高昂" | 原因在语义层，不在结构层 |
| "所有拓扑事件的本质是 Graph 结构重构" | 拓扑事件的本质是**支付签注撤销成本后的语义范式与图结构双重重构** | Semantic IR 层不可跳过 |

### 11.6 双向 Stake 最终标准定义（入术语表）

> **双向 Stake 签注机制**：Stake 在 OIS 语义层表现为签注承诺的沉没成本：S₁-Stake 抬高旧语义签注的撤销代价，锁定旧域稳态、压低 P_pen；S₂-Stake 前置新语义签注的建立代价，为新域稳态提供语义合法性。经由 Semantic IR 双向编译，语义代价转化为 IR 图刚性边约束，最终在 IR 拓扑层实现 Gap 边界加固与拓扑跃迁支撑。所有拓扑事件的本质，是具名智能为了收敛语义层不可弥合的 Gap，主动支付双向 Stake 成本完成的语义范式与图结构双重重构。

### 11.7 与既有体系的对齐

| 既有概念 | 三层架构映射 |
|---|---|
| **OIS v0.2 公理 A1（卡片即对象）** | 卡片是语义层签注承诺的具名载体，经 Semantic IR 编译为 GapBoundary 对象 |
| **OIS v0.2 §六 柜台** | mint = 语义签注的具名确认 + Semantic IR 编译 + IR 层边界状态更新 |
| **osi_law_F1 C3（signer+stake）** | signer = 语义层具名主体；stake = 语义签注承诺成本 |
| **osi_law_F1 C18（非转让核心）** | 名字不可转让 = 语义签注承诺与主体身份/信用/路径深度耦合，无法通过 IR 层操作解绑 |
| **Matt Pocock 补证** | Spec/Tickets 无 stake = 无语义签注承诺成本 → 旧签注零成本撤销、新签注无需前置投入 → IR 层无约束可编译 |
| **合订本 v1.2 卷十 Gaph 数据结构** | GaphEdge.weight 不是"Stake 注入后的刚度"，而是"Semantic IR 编译后的语义签注成本投影" |

---

## 卷十二：体系哲学与自动废弃机制（v1.4.1 增补）

> 承《OIS具名智能｜Gap原语、双向Stake、Semantic IR拓扑演化 完整理论终稿》（2026-09-25 上传）。本节建立 Claim 驱动下旧签注自动废弃的完整机制，并以体系终极设计哲学收尾。

### 12.1 体系终极设计哲学

> **设计是拉近期望和现实的距离。这个距离就是 Gap 的原语和拓扑。**

对 S₂ 的 Stake 是拉近期望和现实的动能；当其完成任务，则退化为 S₂ 的沉没成本，维护现实既有的拓扑对象和其既有的边。

**整套体系的终极逻辑由此闭环**：

1. OIS 具名智能产生认知期望
2. 现实拓扑形成客观实存
3. 二者的语义断层即为一切演化的本源 Gap
4. S₂-Stake 以前置签注成本为动能，击穿旧拓扑边界、收敛语义矛盾、完成系统迭代
5. 跃迁完成后，动态动能转化为稳态沉没成本，固化全新拓扑 Graph 的实体与关联边
6. 形成新的现实稳态，持续抵御下一次拓扑扰动
7. **实现智能系统认知与结构的永续演化**

### 12.2 分支课题：Claim 驱动下旧签注自动废弃机制

#### 12.2.1 课题定位

本分支为 OIS-Gap-Stake 主理论的配套子课题，专门研究拓扑跃迁过程中**旧核心 Endorsement 签注并非由主体主动决策撤销，而是语义约束在 Claim 校验流水线驱动下自动失效**的底层机制。

**区分关键**：主动撤销是主体意向性动作；自动废弃是语义系统内部一致性约束的涌现行为，是 Claim 持续校验、矛盾累积越过临界阈值后的内生结果。

#### 12.2.2 基础概念：Claim 与 Endorsement

- **Claim（断言）**：对现实观测、事实真值的陈述性命题，可被证据校验、裁决，是事实校验流水线的输入对象。Claim 可独立存在，不绑定长期稳态承诺。
- **Endorsement（签注）**：主体长期锚定在稳态拓扑上的承诺性语义结构，附带对应的 Stake 沉没成本。签注构成 S₁/S₂ 稳态的核心认知骨架。

> **关系**：Claim 是校验探针；Endorsement 是承载 Stake、支撑稳态的语义骨架。Claim 的持续反证，逐步侵蚀 Endorsement 的语义合法性，最终触发自动废弃。

#### 12.2.3 自动废弃的完整链式过程

1. **Claim 抽取阶段（Extract）**：系统持续从现实观测中抽取 Claim 断言，与当前 S₁ 的 Endorsement 签注做比对；普通事件带来的微小冲突，可通过 Δ 置信度微调吸收，不触发结构性失效。

2. **冲突累积阶段（Attest）**：持续产出无法被 Δ 消化的反证 Claim，语义冲突不断累积，Gap 不断扩张，S₁ 签注的语义合法性持续衰减，S₁-Stake 的保护效力持续下降，P_pen 逐步抬升。

3. **阈值击穿与自动兑付**：当冲突总量越过语义一致性临界阈值，CEAA 裁决流水线判定现有 Endorsement 集合不可满足；**无需主体发出撤销指令，系统自动兑付 S₁ 签注对应的撤销 Stake 成本，旧核心断言自动废弃**。

4. **语义切换**：旧签注失效后，预加载的 S₂ Endorsement 体系自动接管，S₂-Stake 从跃迁动能，逐步转化为新稳态的沉没成本。

#### 12.2.4 关键边界区分：自动废弃 vs 伪跃迁回弹

| | 伪跃迁 | 自动废弃（有效拓扑跃迁） |
|---|---|---|
| **触发条件** | 短暂 Claim 冲突峰值，未达阈值 | 冲突总量越过语义一致性临界阈值 |
| **S₁ 签注状态** | 并未真正废弃，S₁-Stake 未完成兑付 | 被标记为不可满足，S₁-Stake 成本完成兑付 |
| **系统行为** | 冲突回落后回弹至 S₁ | 旧签注不可恢复，不会自发回弹 |
| **P_pen 变化** | 短暂上升后回落 | 跨越阈值，不可逆跃迁 |

#### 12.2.5 研究核心问题（带入下一轮）

1. 如何定义、量化语义一致性阈值，区分可被 Δ 吸收的局部冲突与触发自动废弃的全局不可满足性？
2. Claim 校验流水线如何计量冲突累积量，该计量指标如何对接 Evolution_rate 演化审计框架？
3. S₁-Stake 撤销成本的自动兑付，在 Semantic IR 层如何映射为 IR 图高刚性边的逐步解除与删除？
4. 不同类型 Claim（局部事实 Claim / 底层本体 Claim）对 Endorsement 签注的侵蚀权重差异——底层本体类 Claim 更容易触发全局自动废弃。
5. 自动废弃的可逆性边界：是否存在部分签注失效、局部拓扑重构的"半跃迁"中间状态？

#### 12.2.6 课题产出预期

1. 建立 Claim-Endorsement 冲突演算模型，给出自动废弃的判定条件；
2. 在 Semantic IR 层，建模"Claim 冲突 → 签注合法性衰减 → Stake 兑付 → 旧图解体"的映射关系；
3. 设计可观测指标，用于区分：普通 Δ 波动、伪跃迁、自动废弃驱动的真实拓扑跃迁；
4. 为 Agent Harness 工程提供落地规范：在 Harness 中植入 Claim 裁决器，模拟、控制自动废弃的触发条件。

---

## 卷十三：麦基《故事》Gap原理的语义拓扑解构（v1.5 新增）

> 承《OIS具名智能｜Gap原语、双向Stake、Semantic IR拓扑演化 完整理论终稿 (2)》（2026-09-26 上传）。本节基于麦基《故事》核心原理，完成GAP原语的底层语义拓扑解构，建立 **Expectation-Event-Conflict-Incentive（E-Ev-C-I）四元核心结构**，为Claim冲突演算、阈值建模、自动废弃机制提供唯一前置理论基础。

### 13.1 麦基核心命题

麦基《故事》核心命题：**主体带着既定认知期望行动，现实事件反馈打破预期，形成不可消解的语义断层（Gap），断层张力生成行动激励，驱动系统迭代演化**。

OIS体系将其形式化、拓扑化，定义GAP原语由**期望（Expectation）、事件（Event）、冲突（Conflict）、激励（Incentive）**四大核心语义节点构成，四者形成闭环有向语义拓扑，是所有语义冲突、Stake变动、拓扑跃迁的本源结构。

### 13.2 四元核心节点标准化定义

#### （1）期望 Expectation（E）—— 语义稳态基准

期望是S₁稳态下，主体核心Endorsement签注集合固化的认知预判模型，是一组绑定S₁-Stake沉没成本的核心Claim集合，定义了主体对现实世界"应当如何"的默认规则。期望自带刚性约束，绑定的Stake越高，推翻预期、废弃签注的撤销代价越大，系统稳态越强。对应麦基理论中主体的固有欲望、目标与认知预设。

#### （2）事件 Event（Ev）—— 现实观测探针

事件是外部现实观测、内部智能推演产生的客观事实实例，会通过Claim-Extract流水线被抽取为标准化事实断言（Claim）。事件分为普通事件与拓扑事件：普通事件仅产生Δ数值扰动，不破坏期望骨架；拓扑事件携带本体级事实偏差，可触发持续性语义冲突。事件是现实映射语义层的唯一载体，无事件则无预期与现实的比对，无Gap生成。对应麦基理论中主体遭遇的客观事态与行动反馈。

#### （3）冲突 Conflict（C）—— Gap本体内核

冲突是事件生成的事实Claim，与期望固化的基准Claim之间，无法通过Δ置信度微调消解的本体级语义矛盾。冲突是Gap的本质内核：局部、可修复的弱冲突仅产生数值波动，不形成有效Gap；全局、不可调和的强冲突持续累积，形成稳定、可演化的OIS-Gap，直接抬升边界可穿透性P_pen。对应麦基理论中预期与现实的核心断裂、认知与事实的根本性错位。

#### （4）激励 Incentive（I）—— 拓扑跃迁动能

激励是Gap强冲突持续存在所产生的语义张力，是驱动系统投入S₂-Stake、重构语义范式、实现拓扑跃迁的核心内生动能。静态冲突仅为语义矛盾，无法驱动系统演化；只有冲突转化为激励，系统才会主动预加载S₂新签注、累积新范式建设成本，为跃迁蓄力。跃迁完成后，激励张力消散，S₂-Stake由动态跃迁动能转化为稳态沉没成本。对应麦基理论中Gap带来的认知压力与行动动机。

### 13.3 GAP四元拓扑闭环有向链路

整套语义拓扑形成自洽闭环，完整复刻麦基演化逻辑，同时适配OIS三层体系：

```
期望(E) → 观测触发 → 事件(Ev) → 事实比对 → 冲突(C) → 张力生成 → 激励(I) → 投入Stake → 新期望(E')
```

1. 系统依托S₁签注体系，固化稳态认知期望，形成语义基准；
2. 现实持续输出观测事件，被流水线抽取为事实Claim；
3. 事实Claim与预期Claim比对，产生不可消解的强冲突，构建有效GAP；
4. GAP持续释放语义张力，转化为迭代激励，驱动系统前置投入S₂-Stake；
5. 激励累积突破阈值，触发旧签注自动废弃、拓扑跃迁，S₂新期望生效，旧Gap收敛，完成一轮系统演化。

### 13.4 三层体系拓扑映射关系

1. **OIS语义层**：期望、事件、冲突、激励为核心语义符号节点，节点有向边承载Endorsement与Claim的约束关系，是Gap演化的本源；
2. **Semantic IR桥接层**：四元语义节点编译为IR拓扑节点，语义冲突强度映射为边权重，激励张力映射为结构重构势能；
3. **IR拓扑层**：当激励势能突破S₁-Stake边界刚性约束，P_pen击穿阈值，触发拓扑事件，完成旧图解体、新图重构。

### 13.5 核心理论边界区分

1. **无事件则无事实比对，无冲突则无有效Gap**——Gap是四元结构的动态演化结果，而非静态距离；
2. **弱冲突可被Δ数值吸收**，仅表象波动，不触发演化；只有本体级强冲突能生成稳定GAP；
3. **冲突是静态语义矛盾，激励是动态演化动能**，二者缺一不可，共同构成Gap的完整演化机制；
4. 本四元拓扑结构为后续Claim冲突演算、阈值建模、自动废弃机制研究提供**唯一前置理论基础**。

### 13.6 四元结构与既有体系的对接

| 既有概念 | 四元结构映射 |
|---|---|
| **S₁-Stake** | 期望(E)绑定的签注承诺沉没成本，抬高期望撤销代价 |
| **S₂-Stake** | 激励(I)驱动的签注前置投入，为新期望(E')提供语义合法性 |
| **普通事件** | 仅产生Δ数值扰动的事件(Ev)，不破坏期望骨架 |
| **拓扑事件** | 携带本体级冲突(C)的事件(Ev)，触发GAP扩张与激励(I)生成 |
| **自动废弃机制** | 冲突(C)累积越过阈值 → 激励(I)突破 → 旧期望(E)自动失效 → 新期望(E')接管 |
| **CEAA裁决算子** | 对冲突(C)强度的裁决，判定是否达到触发激励(I)的阈值 |

---

## 签付页

**本文档性质**：拓扑边界 Gap 原语体系升级合订本（v1.5）
**签付者**：wend4336
**签付日期**：2026-09-25
**签付动作**：具名签署，确认以下内容——

1. Gap 从差值（Δ）升级为拓扑边界（∂），作为 OSI/OIS 的理论基线；
2. Stake 从风险覆盖/结构刚度升级为双向签注承诺成本（S₁-Stake 抵抗跃迁 + S₂-Stake 支撑跃迁），语义层为本体，IR 拓扑层为投影；
3. Handle 从值引用升级为边界状态封装；
4. CEAA 裁决算子从 V_judge = f(Δ) 升级为 V_judge = f(G_state, P_pen, Stake_real, Threshold)；
5. v1.1 增补：事件类型学——拓扑事件（边界穿透）与普通事件（状态波动）的严格区分，作为边界状态机与事件路由的理论基线；
6. v1.2 增补：Gaph 图论视角——拓扑事件等价于 Graph 结构重构，普通事件等价于边权重修改，为系统提供可计算的数据结构基线；
7. v1.3 增补：三层架构——OIS语义层→Semantic IR→IR拓扑层，语义签注成本为本体，IR结构为投影，双向Stake闭环；
8. v1.4.1 修订：删除案例部分，保留体系终极设计哲学、Claim 驱动旧签注自动废弃机制；
9. **v1.5 新增：麦基《故事》Gap原理的语义拓扑解构——Expectation-Event-Conflict-Incentive四元核心结构，为Claim冲突演算、自动废弃机制提供唯一前置理论基础；**
10. 本合订本卷一至卷十三的全部修正、裁定与规格，作为 v0.3 的理论输入；
11. 开放问题 ×8 + Claim 自动废弃研究问题 ×5 + 四元结构研究问题（阈值量化、冲突演算、侵蚀权重）带入下一轮，不阻塞 P0 启动。

**Stake**：本签付的权威性绑定于签付者的研究信用；若后续工程验证推翻本合订本的核心假设（如 P_pen 多因子模型、事件类型判定器、Gaph 数据结构、双向 Stake 三层架构、Claim 自动废弃机制或麦基四元语义拓扑结构被证明不可行），签付者承担理论修正责任。

**Handle**：`oi:compendium/topology-gap-v1.5/20260926`

---

> （注：本合订本由 R1（Kimi 2.8 对齐研究）与 R2（Kimi 3 进阶研究）合并编纂；v1.1 增补事件类型学；v1.2 增补 Gaph 图论视角；v1.3 增补 Stake 双向原语与三层架构；v1.4.1 修订：删除案例，保留哲学与自动废弃机制；v1.5 新增：麦基《故事》Gap原理的语义拓扑解构——E-Ev-C-I四元核心结构。全部断言均来自用户提供的既有文档及本报告的理论推演。）
